import React from "react";
import Button from "./Button";

export default {
    title: "Progress/Button",
    component: Button,
};

export const ColorProgreso = () => (
    <Button variant="colorProgreso" />
);

export const ColorRanking = () => (
    <Button variant="colorRanking" />
);

export const SolicitarDesafio = () => (
    <Button variant="solicitarDesafio" />
);

export const NotSelectedRanking = () => (
    <Button variant="notSelectedRanking" />
);

export const SolicitudEnviada = () => (
    <Button variant="solicitudEnviada" />
);

export const NotSelectedProgress = () => (
    <Button variant="notSelectedProgress" />
);